const authStyles = (theme) => ({
	root: {
		flexGrow: 1,
		height: "100vh",
	},
	container: {
		padding: "40px",
		marginTop: theme.spacing(2),
	},
  link: {
    cursor: "pointer",
  }
})
export default authStyles
